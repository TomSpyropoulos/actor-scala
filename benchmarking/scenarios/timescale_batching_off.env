# Batching sweep — single-row inserts, pool 20, 20 publishers. Reference point for why the anchor batches; the write path saturates here.
DB_BACKEND=timescaledb
DB_URL=jdbc:postgresql://timescaledb:5432/epu
DB_USER=postgres
DB_PASSWORD=postgres
DB_POOL_SIZE=20
BATCH_ENABLED=false
PUBLISHER_COUNT=20
