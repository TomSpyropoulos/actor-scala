# Pool sweep — large pool (50 workers), no batching, 5 publishers (~5k msg/s)
DB_BACKEND=timescaledb
DB_URL=jdbc:postgresql://timescaledb:5432/epu
DB_USER=postgres
DB_PASSWORD=postgres
DB_POOL_SIZE=50
BATCH_ENABLED=false
PUBLISHER_COUNT=5
