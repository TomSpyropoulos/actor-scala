# Payload size sweep — 1KB padding per message, no batching, 5 publishers (~5k msg/s)
DB_BACKEND=timescaledb
DB_URL=jdbc:postgresql://timescaledb:5432/epu
DB_USER=postgres
DB_PASSWORD=postgres
DB_POOL_SIZE=20
BATCH_ENABLED=false
PUBLISHER_COUNT=5
PAYLOAD_PADDING_BYTES=1024
